问题
    分表
        页面保存无效果
            分表被清空
            update _a, 不管row 了
    签署待办
        纸质pdf待办
            问题
                先发送待办, 然后word 转 pdf 失败
                导致用户啥内容都没有
            解决
                先转pdf 再发送待办
        dubbo问题
            问题
                上游是 ILocalString, 接收用 ILS, cast异常
                接收时,变成HashMap
            解决
                使用 HashMap 接收
        移动端点击慢操作
            问题
                多次点击卡, 后面API 异常
            解决
                慢操作加锁
        电子信息确认word转pdf部分失败
            问题
                EDM 附件系统不稳定
                电子信息确认word转pdf部分失败
                导致部分失败 没有重试功能
            解决
                转PDf 拆出独立 接口
    升级
        准备检查
            前端
            平台
            业务模块Release
        变更准备
            修改配置
        变更
            停应用
            数据库备份
            升级
                MC不停
                    配置发布
                刷升级脚本
                部署最新包
        验证
    MySQL 的隐式转换
        两个参数至少有一个是 NULL 时，比较的结果也是 NULL，例外是使用 %26lt;=%26gt; 对两个 NULL 做比较时会返回 1，这两种情况都不需要做类型转换
        两个参数都是字符串，会按照字符串来比较，不做类型转换
        两个参数都是整数，按照整数来比较，不做类型转换
        十六进制的值和非数字做比较时，会被当做二进制串
            子主题
        有一个参数是 TIMESTAMP 或 DATETIME，并且另外一个参数是常量，常量会被转换为 timestamp
        有一个参数是 decimal 类型，如果另外一个参数是 decimal 或者整数，会将整数转换为 decimal 后进行比较，如果另外一个参数是浮点数，则会把 decimal 转换为浮点数进行比较
        所有其他情况下，两个参数都会被转换为浮点数再进行比较
            有例外
                子主题
